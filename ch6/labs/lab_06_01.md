
任務：完成一個 `createDiscountCalculator(memberLevel)` 函數。

這個函數會根據會員等級回傳不同的「折扣計算函數」：
- `"vip"` 會員：8 折
- `"gold"` 會員：9 折
- 其他會員：不打折

完成後，以下程式碼應該可以正確執行：

```javascript
const vipDiscount = createDiscountCalculator("vip");
const goldDiscount = createDiscountCalculator("gold");
const normalDiscount = createDiscountCalculator("normal");

console.log(vipDiscount(1000)); // 800
console.log(goldDiscount(1000)); // 900
console.log(normalDiscount(1000)); // 1000
```

請將以下程式碼片段重新排列，組合成正確的函數。

```javascript
}
```

```javascript
return price => price * 0.8;
```

```javascript
if (memberLevel === "vip") {
```

```javascript
return price => price;
```

```javascript
function createDiscountCalculator(memberLevel) {
```

```javascript
if (memberLevel === "gold") {
```

```javascript
return price => price * 0.9;
```

```javascript
}
```

```javascript
}
```

提示：
- `createDiscountCalculator` 是一個會「回傳函數物件」的函數
- `price => price * 0.8` 是箭頭函數，等同於：

```javascript
function(price) {
    return price * 0.8;
}
```

