# Chapter 12 Part 1 學習單

Q1. 簡要說明以下 BOM 的元件的用途與功能
- `window`:
- `document`:
- `Location`:

Q2. 說明什麼是 `DOM`? 並將以下的 HTML 文件 轉換成 DOM 樹狀結構
```html
<!DOCTYPE html>
<html>
  <head>
    <title>Document</title>
  </head>
  <body>
    <h1>這是標題</h1>
    <p>這是段落</p>
    <ul>
      <li>項目 1</li>
      <li>項目 2</li>
    </ul>
  </body>
</html>
```

Q3. `document` 物件提供那些方式讓開發者彈性的選取 DOM 樹狀結構中的元素? 說明這些方式和相對應的 document 物件的方法。

Q4. DOM 模型中的 `Node` 物件和 `HTMLElement` 物件有什麼關係?

Q5. 單字 Sibling 是什麼意思？ `element.nextElementSibling` 和 `element.previousElementSibling` 可以用來做什麼?