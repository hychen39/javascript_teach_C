# Chapter 12 Part 2 學習單

Q1. 如何取得 HTMLElement 物件中套用的所有 CSS 類別(CSS class) 及其 行內的 CSS 樣式?

Q2. HTML tag 中的 `data-*` 屬性可以用來做什麼? 在 JavaScript 中如何取得這些屬性?

Q3. 在什麼情況下必須使用 `setAttribute()`, `getAttribute()` 和 `removeAttribute()` 方法去修改 HTML 標籤上的屬性?

Q4. 在 DOM 中新增元素的程序是什麼?
